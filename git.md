#### rep에 폴더 추가하기
```
$ git clone https://github.com/skyrosepark/lectures lectures
cd lectures
git status
git add Reading/
git status
git commit -m "add a folder Reading"
git remote-v
git push origin master
```
